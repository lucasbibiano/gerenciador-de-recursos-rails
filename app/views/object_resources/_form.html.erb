<%= simple_form_for(@object_resource) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :description %>
    <%= f.input :tombamento %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
